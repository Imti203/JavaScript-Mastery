var variableName = 'welcome to the variables'; // it's a old variable, and this time it's not uses 
let first_Name = 'Jhon'; // this variable changeable 
const last_Name = 'kate'; // this variable not changeable 

console.log(variableName);
console.log ( first_Name, last_Name);

// Variable Naming
// 1. The name should be uniqe
// 2. The name should not be any reserve keyword
// 3. The name must start with a character, _ and $ sign 